name: Python CI
on: [push, pull request]
jobs:
  build:
  runs on: ubuntu-latest 
  steps:
    - uses: actions/checkout v2
    - name: set up Python
    uses: actions/setup=python@v2 
    with:
    python-version: 3.x names Install dependencies
  run: pip install -r requirenents.txt 
  -name: Run tests 
  run: Pytest
